// the method is to call the potention functions
    def append(T value);
        // first defination it says that phi of empty equal zero
        // consider an operation ont the data structure below:
    def flush();
    def foreach();
        // s(empty) and the afterward is 
        // s(post)
        // c(cost of operation)

        // this equation define the amortized cost c prime
        // think about our potential fucntion is actually doing
        // the amortized cost is less than big of n
    def T min();
  